package utils
{
	import flash.geom.Rectangle;

	public class RectangleUtil
	{
		public static function setTo(rec:Rectangle,x:Number,y:Number,w:Number,h:Number):Rectangle
		{
			rec.x=x;
			rec.y=y;
			rec.width=w;
			rec.height=h;
			return rec;
		}
	}
}