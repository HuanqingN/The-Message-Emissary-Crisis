package extension.skills;

import extension.cons.StepCons;
import extension.vo.SkillVO;

/**ÌìÒÂÎŞ·ì**/
public class Skill67 extends Skill {

	@Override
	public Boolean check() {
//		if(getOwner().skillhash.get(this.id).launchCount<1 && getOwner().isOpen && bf.nowPlayer.getUid()==getOwner().getUid()){
//			if(bf.nowStep!= StepCons.InfoReceive)return true;
//		}
		return false;
	}

	@Override
	public void play(SkillVO tvo) {
		super.play(tvo);
	}
	
}
